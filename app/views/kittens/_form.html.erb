<%= form_with(model: kitten) do |form| %>
  <% if kitten.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(kitten.errors.count, "error") %> prohibited this kitten from being saved:</h2>

      <ul>
        <% kitten.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div>
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>
    <div>
      <%= form.label :age %>
      <%= form.number_field :age, in: 0..42, step: 1 %>
    </div>
    <div>
      <%= form.label :cuteness %>
      <%= form.check_box :cuteness %>
    </div>
    <div>
      <%= form.label :softness %>
      <%= form.check_box :softness %>
    </div>
    <%= form.submit %>
  </div>
<% end %>
